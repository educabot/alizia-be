version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert
    - bodyclose
    - nilerr
  exclusions:
    paths:
      - docs/.*
      - db/.*
      - scripts/.*

formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/educabot/alizia-be
